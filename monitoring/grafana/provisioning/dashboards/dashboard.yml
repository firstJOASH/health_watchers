apiVersion: 1

providers:
  - name: "Health Watchers"
    orgId: 1
    folder: "Health Watchers"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards

apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: health-watchers
  labels:
    app.kubernetes.io/name: health-watchers
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
